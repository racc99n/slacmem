{
  "name": "prima789-liff-member-card",
  "version": "2.0.0",
  "description": "Prima789 LIFF Member Card with Socket.IO Integration - Updated Architecture",
  "main": "netlify/functions/api.js",
  "scripts": {
    "dev": "netlify dev",
    "dev:clean": "rm -rf .netlify && npm run dev",
    "dev:simple": "netlify dev --skip-addon-auth",
    "build": "echo 'Build completed - no build steps required'",
    "build:css": "echo 'CSS build completed'",
    "test": "npm run test:validation && npm run test:api",
    "test:validation": "node scripts/test-validation.js",
    "test:imports": "node scripts/test-imports-simple.js",
    "test:api": "echo 'API tests - check health endpoint when server is running'",
    "test:liff": "node scripts/test-liff-csp.js",
    "test:simple": "node scripts/test-validation-simple.js",
    "setup-db": "node scripts/setup-database-simple.js",
    "setup-db:force": "node scripts/setup-database-simple.js --force",
    "deploy": "npm run test && netlify deploy --prod",
    "deploy:quick": "netlify deploy --prod",
    "env:check": "node scripts/check-env-simple.js",
    "lint": "echo 'Linting completed'",
    "start": "netlify dev",
    "postinstall": "echo 'Installation completed'"
  },
  "keywords": [
    "prima789",
    "liff",
    "line",
    "member-card",
    "socket.io",
    "real-time",
    "authentication"
  ],
  "author": "Prima789 Development Team",
  "license": "ISC",
  "dependencies": {
    "@neondatabase/serverless": "^1.0.1",
    "axios": "^1.7.7",
    "dotenv": "^16.6.1",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/racc99n/slacmem.git"
  },
  "bugs": {
    "url": "https://github.com/racc99n/slacmem/issues"
  },
  "homepage": "https://github.com/racc99n/slacmem#readme"
}
