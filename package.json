{
  "name": "prima789-liff-member-card",
  "version": "2.0.0",
  "description": "Prima789 LINE LIFF Member Card Integration",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Build completed - no build steps required'",
    "start": "netlify dev",
    "dev": "netlify dev --port 8888",
    "test": "npm run test:validation && npm run test:api",
    "test:validation": "node scripts/test-validation.js",
    "test:api": "node scripts/test-api.js",
    "test:all": "npm run test:validation && npm run test:api && npm run test:integration",
    "test:integration": "node scripts/test-integration.js",
    "setup-db": "node scripts/setup-database.js",
    "env:check": "node scripts/check-env.js",
    "deploy": "npm run test && netlify deploy --prod",
    "deploy:preview": "netlify deploy",
    "logs": "netlify functions:log api",
    "functions:serve": "netlify functions:serve"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/node": "^20.19.14",
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/prima789-liff.git"
  },
  "keywords": [
    "LINE",
    "LIFF",
    "Prima789",
    "Member Card",
    "Netlify",
    "Node.js"
  ],
  "author": "Your Name",
  "license": "MIT",
  "netlify": {
    "functions": {
      "directory": "netlify/functions",
      "node_bundler": "esbuild"
    }
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead"
  ]
}
